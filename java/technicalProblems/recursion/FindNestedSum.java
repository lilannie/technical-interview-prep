package technicalProblems.recursion;

/*

function findNestedSum(list, depth) {
    var sum = 0;
    for (var i = 0; i < list.length; i++) {
        if (Array.isArray(list[i])) {
            sum += findNestedSum(list[i], depth+1);
        }
        else {
            sum += list[i] * depth;  // 1 //
        }
    }
    return sum;
}

console.log(findNestedSum([1,[4,[6]]], 1));

 */
public class FindNestedSum {

}
